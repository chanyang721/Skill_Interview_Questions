### Node.js가 무엇인가요 ? 
* Nods.js는 V8엔진과 libuv를 이용하여 웹 서버에서 JavaScript를 비동기적으로 사용할 수 있도록 해주는 런타임 환경(프로그램)입니다.
* 특히 node.js는 libuv 라이브러리로 Non-blocking I/O와 단일 스레스 방식의 이벤트 루프를 사용하여 높은 처리 성능을 가지고 있는게 특징입니다.
* libuv is a multi-platform C library that provides support for asynchronous I/O based on event loops.
### Node.js를 사용하는 이유 ? 
* JavaScript라는 한 가지 언어로 웹 페이지를 만들기 위해 Node.js를 사용한다고 생각합니다. 
* 블로깅 후 추가 

### OSI 7계층

### XSS

### CFRS

### singleton 패턴이란 ?

### URL과 URI의 차이점

### node.js의 이벤트 루프가 뭔가요. nodejs에서 비동기 처리를 그림으로 그려서 설명할 수 있나요?
* 이벤트 루프는 싱글 스레드 기반 언어인 자바스크립트에 동시성을 부여하기 위한 개념입니다. 
* 비동기적인 작동처럼 보이는 동시성을 웹 애플리케이션에 부여하기 위해 자바스크립트의 V8엔진에 있는 Call Stack이라는 하나의 스레드와 node.js에서 비동기를 구현하기 위한 libuv 라이브러리를 통해 동시성을 부여하며, 이벤트 루프는 비동기 요청을 마치고 Queue에서 대기중인 콜백함수를 Call Stack으로 우선 순위에 따라 넣어주는 역할을 하기 때문에 node.js에서 동시성을 부여하기 위한 핵심적인 기능을 하고 있습니다.

nodejs에서 callback을 설명해 보세요
* 콜백이란 나중에 호출된다라는 의미로 콜백 함수란 나중에 호출되는 함수를 말합니다. nodejs에서 콜백함수는 비동기적인 프로그래밍을 하기 위해

nodejs에서 비동기란 무엇인가요
* 여러가지 작업을 동시에 처리하는 것을 비동기라고 합니다.

nodejs callback hell이 무엇인가요? 어떻게 이를 피할 수 있을까요?
* 

nodejs에서 module 이란 무엇인가요?
* 모듈이란 특정한 기능을 나누어 하나의 함수로 만든 것을 모듈이라고합니다.

nodejs에서 event loop이란 무엇인가요?
* 

nodejs에서 비동기 처리를 그림으로 그려서 설명할 수 있나요?
* 

chrome이랑 node.js환경 다른점
* 우선 크롬과 nodejs의 다른점은 사용하는 목적이 다릅니다.
* 크롬 같은 경우 HTML, CSS, JS를 실행하여 웹 페이지 화면을 렌더링하는 것이 주된 목적이기 때문에 제공하는 API가 DOM, Fetch와 같은것을 지원합니다. nodejs는 서버 개발 환경을 제공하는 것을 목적으로 하기 때문에 Crypto, Path와 같은 것을 지원하게 됩니다.
* 

--- 

session - cookie 방식과 토큰 방식의 차이점에 대해 이야기해보세요

세션에 대해서 모르는 사람한테 설명하듯 간단하게 설명해 주세요.

### 서버 측에서 쿠키를 저장할때 클라이언트에 어떤 헤더를 통해 전달될까요.
* Set-cookie라는 헤더를 통해 전달됩니다.

### jwt 토큰을 사용하신 이유는 뭔가요. 어떻게 구성되어 있나요.
* jwt 토큰을 사용하는 이유는 stateful한 상태를 유지시키기 위한 유저의 인증 정보를 암호화 하기 위해 사용됩니다.
jwt 토큰을 사용하는 이유는 유저의 정보를 암호화하여 클라이언트와 서버간에 stateful한 상태를 유지시키기 위해 사용합니다.

* stateless한 서버의 특성상 요청으로 받은 state값을 따로 저장하지 않기 떄문에 로그인 인증과 같이  jwt의 구성으로는 header, payload, 그리고 signature의 3부분으로 나누어져 있으며, header에는 토큰 종류, 암호화 알고리즘의 종류가 저장되어 있고, payload에는 유저 정보, 권한과 같은 정보를 넣게 되며, 시그니쳐 부분에는 헤더와 페이로드를 인코딩한 값과 솔트를 적용한 암호화된 값이 포함되어 있습니다.

세션과 토큰 중의 각각 장단점을 말씀해주세요.
* 세션은 유저 인증 방식으로 유저의 정보를 서버의 세션스토리지에 저장하고 클라이언트에게는 세션아이디만 전달하여 세션아이디를 통해 유저를 인증하는 방식입니다.
* 그렇기 때문에 세션의 장점과 단점은 서버에 유저 정보를 저장한다는 특징에서 말할 수 있다고 생각합니다. 우선 장점으로는 서버에서 유저 정보가 있기 때문에 추가적인 확인이 가능하다는 것이구요. 단점으로는 서버의 저장 공간을 차지하기 서버에 무리를 줄 수 있습니다. 또한, 세션아이디를 쿠키를 사용하여 전달하기 때문에 XSS공격을 당할 가능성이 있습니다.
* 토큰도 유저 인증을 위한 방법이지만, 세션과는 다르게 서버에서 암호화된 토큰을 클라이언트에 저장하여 해당 토큰의 유효성을 통해 유저 인증하는 방법을 사용하고 있습니다. 
* 토큰의 장점으로 암호화된 토큰만을 사용하여 유저 인증을 진행하기 떄문에, 인증을 위한 저장 공간이 없다는 것과, 이미 암호화된 토큰을 주고 받기 때문에 암호화 키를 노출시키지 않는다.
* 하지만, 단점으로는 토큰은 로그아웃 기능이 없기 떄문에 만료시간으로 컨트롤 해야한다는 것이 있습니다.

세션과 토큰 서로 trade-off 할 수 있는 관계인가요.

세션 대비 jwt 토큰의 보안적인 차이가 있나요.

세션은 서버의 세션 스토어를 사용하기 때문에 메모리 부하가일어나는데 jwt는 그럼 해독할때 cpu 리소스가 들지않을까요? 

둘 중 어느 것이 더 낫다고 보시나요.

메모리를 쓰는게 더 나은가요? 아니면 cpu를 쓰는게 더 낫다고 보나요.

토큰에 있는 내용을 암호화 시켜야될 이유는 뭔가요.

* 세션스토어는 물리적으로 어디 저장되어있나요.
* 세션 스토어의 경우 객체의 형태로 물리적으로 MemoryStore라는곳에 저장됩니다. 메모리에 저장된 데이터는 휘발성을 가지고 있기 때문에 중요한 정보를 저장하는 장소로는 부적절합니다.
* 이를 해결하기 위해, 세션 정보를 담은 객체를 파일에 저장하거나, mysql을 통해 저장하는 방법이 있다고 알고 있습니다.

만약 서버가 두개로 분산되어있는데, 서버앱의 세션스토어도 분리될텐데 a서버앱을 통해서는 자동로그인이 되는데 b서버앱을 통해서는 자동로그인이 안될수도 있지 않을까요.

---


### Node.js가 싱글스레드인 이유와 장점을 설명해주세요 (중요) 
* nodejs가 싱글스레드인 이유는 자원에 대한 동시성을 신경쓰지 않기 위해 싱글 스레드로 만들었습니다. 그리고 nodejs의 장점으로는 자바스크립트를 알고 있다면 새로운 언어를 익히지 않고 서버를 만들수 있다는 것과, 싱글 스레드를 기반으로 백그라운드에서 멀티 스레드로 이벤트를 처리하는 방식을 사용하여 비동기를 구현했기 때문에 서버에 무리를 주지 않습니다. 또한 npm을 통해 다양한 모듈을 제공받아 사용할 수 있다는 것이 있습니다. 

단점이 있다면, 싱글 스레드이기 때문에 하나의 작업 자체가 오래 걸리는 서비스에는 어울리지 않다는 것과, 인터프리터 언어이기 때문에 에러가 나느 경우 프로세스 자체가 꺼지기 때문에 반드시 모든 경우에 대한 소스코드의 테스트를 병행하면서 개발을 해야 한다는 것이 있습니다.
### Node가 싱글 스레드인데 멀티 스레드보다 빠른 이유를 설명해주세요.
* 싱글 스레드는 동기화 작업을 하지 않아도 되기 때문에 멀티 스레드보다 빠릅니다.
* 멀티스레드를 사용하는 경우 공유 자원으로 인한 문제 해결을 위해 동기화에 신경을 써야합니다. 하지만, 싱글 스레드는 이 동기화에 신경쓰지 않아도 되기 때문에 멀티 스레드보다 빠르다고 알고 있습니다. 

### package.json의 역할은 무엇인가요?
* 해당 프로그램을 실행시키는데 필요한 npm의 모듈과 버전을 명시해 놓은 파일로 협업 시 반드시 필요한 모듈을 정확하게 명시합니다. 이는 npm install 명령어로 손쉽게 프로그램을 실행하기 위한 모듈을 다운받을 수 있도록 하는 역할을 하고 있습니다.
* package.json의 중요한 구조로는 dependencies와 devDependencies 그리고 srcipts가 있습니다. 
* dependencies에는 프로그램 동작에 반드시 필요한 모듈들을 넣고, devDependencies에는 프로그램 동작에 영향을 주지 않지만 필요한 모듈들을 넣습니다. 
* srcipts에는 CLI 환경에서의 명령어를 작성하여 "npm + 명령어"를 입력할 시 해당 명령어의 value로 작성한 문자열을 실행됩니다.

### Get,Post 메소드 차이점 (중요)
* Get과 Post의 차이점은 HTTP 요청을 통해 데이터베이스의 값의 변경 유무라고 할 수 있습니다. 
* Get 메소드는 HTTP 요청 시 단순히 요청에 의한 값을 받아기 위해 사용되지만, Post 메소드는 이와는 반대로 반드시 HTTP 메시지의 Body 부분에 추가하고자 하는 값을 첨부하여 서버단에 있는 저장소에 해당 데이터를 추가하기 위한 목적으로 사용되는 메소드입니다.

### HTTP 와 HTTPS 차이점 (중요)
* HTTP와 HTTPS의 가장 큰 차이점은 HTTP 요청 메시지에 암호화를 적용 했는지 안했는지 여부입니다.
* HTTP는 클라이언트와 서버가 데이터를 주고 받기 위한 프로토콜 중 하나입니다. HTTP는 서버에 암호화 되지 않은 요청 메시지를 서버로 전달합니다. 이런 경우 데이터가 날것 그대로 전달되기 때문에 중간에 요청 메시지가 유출 될 위험이 존재합니다.
* HTTPS는 HTTP에 암호화가 추가된 프로토콜로 데이터를 암호화하여 만약 데이터가 중간에 유출되어도 복호화키가 없다면 어떤 데이터인지 알 수 없도록한 방식입니다. 
* HTTPS는 암호화를 위해 인증서와 한쌍의 복호화 키를 가지고 진행됩니다. 서버가 클라이언트로부터 요청을 받는다면 인증서, 응답 객체, 공개키를 클라이언트에게 보내게 됩니다. 응답 받은 클라이언트에서는 인증서에 적히는 도메인이 자신이 보낸 도메인과 같은지를 비교하여 데이터가 중간에 다른 도메인을 거쳐서 자신에게 도착했는지 확인할 수 있게 됩니다.
* HTTPS는 공개키와 비밀키 개념을 기반으로 하는 비대칭 키 암호화 방식을 사용합니다. 클라이언트와 서버 각각 한쌍의 키를 가지고 있고, 만약 클라이언트가 서버에게 메시지를 보내고 싶은 경우 hand shake단계에서 각자의 공개키를 서로에게 전달 받게 됩니다. 그 후, 전달 받은 공개키로 데이터를 암호화하여 http메시지를 서로에게 보내게 됩니다. 서버는 자신의 공개키로 암호화된 클라이언트의 메시지를 개인키로 복호화하여 전달 받은 메시지를 읽을 수 있게 되는 단계로 진행 됩니다.

### http status code 의 200대 300대 400대 500대 마다 각각 어떤목적을 가진 응답코드인가요.

### http 대표 메소드 4가지 설명해주세요.

### get, post, put, patch, delete에 대해서 알려주세요.

### put과 patch는 어떤차이가 있나요.

### cors정보가 http header에 어떻게 표시되던가요.

### 프론트에서 http요청이 많은 것이 좋은가요, 적은것이 좋은가요.

---

### Express에 대해서 설명해주세요.

### OOP가 무엇인가요.

### 캡슐화가 어떤건지 설명해주세요.

### React 장점이 무엇인가요. 

### 조인이 뭔가요. Inner join과 outer join의 차이는 무엇인가요.

### 배포를 해봤을 텐데, 트래픽이 많아지면 서버가 과부화가 걸립니다. 이에 대한 대처 방법이 무엇인가요.

---

### 자료구조랑 운영체제에 대해서 설명해주세요

배열과 링크드리스트의 차이점에 대해서 설명해주세요.

왜 링크드리스트가 배열에 비해 수정/삭제가 더 편한지 설명해주세요.

### 에자일이 뭔가요.

### 동기 비동기 차이는 무엇인가요.

### db인덱스는 어떤 자료구조로 되어있나요.
https://lalwr.blogspot.com/2016/02/db-index.html


### server의 역할에 대해 이야기해보세요 -->

### 싱글톤 패턴이란 ? 
* 싱글톤 패턴은 자주 사용하는 객체를 하나만 생성하는 것을 보장하고, 생성된 객체를 프로그램의 어디서나 접근할 수 있도록 만든 디자인 패턴입니다.
* 내용 추가

### 프로세스와 스레드 차이는 무엇인가요.
* 프로세스는 메모리상에서 실행중인 프로그램을 말하며, 스레드는 이 프로세스 안에서 실행되는 흐름의 단위입니다.
### 싱글 스레드와 멀티 스레드 차이점은 무엇인가요.
* 싱글 스레드와 멀티 스레드의 차이점은 동기적, 비동기적인 작업 흐름입니다. 싱글스레드는 작업 흐름이 하나이며, 멀티스레드는 스레드의 개수 만큼 가지고 있습니다.
* 작업의 흐름의 단위가 하나인 싱글 스레드는 하나의 작업이 끝난 뒤에 두번째 작업을 시작하는 동기적 흐름을 가지고 있는 반면, 멀티 스레드는 스레드의 개수만큼 작업을 비동기적으로 진행할 수 있다는 차이점이 있습니다.
* 하지만, 속도에서는 큰 차이가 없게 되는데요. 작업하는 흐름의 단위만 다를뿐 작업 속도는 큰 차이가 없기 때문에 실제 작업이 완료되는 속도는 비슷하다고 알고 있습니다.

### 배포 진행 여부 (중요)

### CI/CD 구현 여부 (중요)

### 브라우저 작동 원리 (중요)

### 서버 사이드 랜더링 개념 (중요)
---------------------------------------------------------------



SPA를 할 때 무엇을 사용했나요.

HTML CSS의 관계를 설명해주세요.

백엔드를 지원했는데 프론트에서 어떤 부분이 어렵게 느껴졌는지 말씀해주세요.

완벽히 바닐라JS로 구현한 프로젝트가 있으신가요.

Well-known port foward에 대해 설명해주세요.

프로젝트에서 스키마를 짤 때 어떤 기준으로 만들었는지 말씀해주세요.

키값은 뭘로 지정했나요.

FK키의 사용 장점은 무엇인가요.

리눅스 사용은 해 본 경험이 있나요.

해쉬랑 해쉬맵에 대해 설명해주세요.

자바스크립트와 타입스크립트 차이는 무엇인가요.

마이크로서비스 아키텍처가 뭔가요.

비동기처리 중 async/await 로 해결 안되는건 어떻게 하시나요.(답: 강제로 프로미스 반환)

MVC 패턴에 대해서 알고있나요? 설명해주세요.

### REST API가 무엇인지 설명해주세요.
* REST는 Representational State Transfer의 약자이고 API는 Application Programming Interface의 약자입니다. 우선 API는 사람이 가게에 가서 음료수를 주문하는 경우에 보게 되는 매뉴판에 비교할 수 있으며, 사람이 아닌 프로그램 간 주문하기 위한 매뉴판을 API라고 합니다.
* 앞에 REST라는 단어가 붙은 이유는 프로그램 간 사용하는 매뉴판이지만 결국 사람이 작성하고 읽어야 하기 때문에 서로간에 작성 규칙을 적용하여 누가 읽어도 이해할 수 있도록 하기 위해서 입니다. 따라서, RESTful 하다는 말은 이 규칙이 잘 적용된 REST API를 뜻합니다.
* REST의 가장 중요한 특징은 각 요청(주문)은 "요청 자체로 어떤 기능을 하는지 추론이 가능해야한다"라는 원칙을 가지고 있습니다. 이를 위해 API 요청을 구성하는 자원(resource)을 URL, 행위(Verb)를 HTTP Method, 그리고 표현(Representations)으로 구분하였고, 각각 규칙을 만들어 알맞게 적용한 API를 RESTful API라고 합니다.
* 사용시 장, 단점 채우기

TCP와 UDP의 차이점에 대해서 아는대로 설명해주세요.

자료구조에서 Binary Tree의 3가지 종류와 특징을 설명해주세요.

비동기처리 방식에 대해서 설명해주세요.

cors에서 origin이 뭔가요.
* cors의 origin은 요청을 허가하는 클라이언트의 URL입니다.

* CORS란 무엇이고 목적은 어떻게 되는지 설명해주세요. 그리고 NodeJS 기반의 웹 서버에서 CORS를 어떻게 설정하는지 설명해주세요.
CORS (Cross-Origin-Resource-Sharing)의 약자로 한글로 해석하자면 교차 출처 자원 공유로 할 수 있습니다. CORS는 기존의 웹 브라우저의 설정과는 반대로 클라이언트의 URL과 서버의 URL이 달라도 클라이언트의 요청을 서버에서 허가할 수 있는 설정입니다. 

* 기존의 웹 브라우저는 사용자가 브라우저 주소창에 URL을 작성하여 요청을 서버에 넣는다면 Server에서 비즈니스 로직과 더불어 HTML 문서까지 반환하는 방식인 SOP(Same Origin Policy)인 동일 출처 정책을 적용하는 것이 가장 일반적인 방법이었습니다. 이에 따라 웹 브라우저에서 만약 다른 도메인에서 요청이 들어오는 것은 보안상 위험한 문제로 인식하는 시기 였기 때문에 기본적으로 웹 브라우저에서 해당 요청을 자체를 막는 설정이 적용되어 있었습니다. 하지만, 웹사이트가 발전 할수록 서버에서 단순하게 HTML 문서를 반환하는 일 뿐만 아니라 사용자와의 인터렉션에 대한 요구가 크게 증가하게 되었습니다. 이러한 요구는 다양한 데이터를 사용자에게 제공하기 위해 기존에 같은 출처에만 요청을 보내는 것보다 다른 출처에 방대한 데이터를 요구하는 수요가 크게 증가하게 되었습니다. 이러한 요청 공식적으로 셋팅하는 방법이 CORS 설정이라고 알고 있습니다.  

* Node.js 기반의 웹서버에서 CORS를 설정하는 방법은 ? 
제가 구축했던 express를 기반 서버에서 cors 설정을 위해 사용한 방법은 매우 간단해습니다. express라는 라이브러리에서 use라는 메서드로 cors 미들웨어 함수를 실행하였습니다. 매계변수 자리에 origin이나, 요청 메서드 등의 설정을 적용하면 손쉽게 cors가 적용되었습니다. 특히 origin부분에 출처인 URL을 적용해야 하는데 주의해야 했던 부분은 프로토콜 & 호스트 & 포트를 요청하는 URL과 일치시켜야 요청을 허가했습니다.

만약 naver에서 facebook의 api를 get으로해서 호출 할 수 없나요.

만약 origin을 ###로 설정하면 어떻게 되는건가요.

http 스펙의 메소드가 어떤게 있을까요.

option 메소드는 언제쓰이나요.

그럼 option을 호출하는 이유는 뭐고 option을 요청하면 받는 응답은 뭔가요.

head 메소드는 무슨의미인지 아시나요.

<!-- nest.js는 왜 사용하셨나요. -->

프로젝트 관련해서 설명해주세요.

OAuth2.0의 워크플로우에대해서 한번 설명해주세요.

혹시 authorizationCode라는 grant type말고 다른 grant type도 써보셨나요.

그러면 인덱스의 time complexity는 어떻게 되어 있나요.

인덱스에서 성능향상이 어려운경우는 어떤 경우인가요.

로드 밸런싱을 해보신 적 있나요.

<!-- nest.js를 선택한데 있어서 어떤 커뮤니케이션이 있었고, nest.js를 사용하는데 있어서 장단점은 무엇인지 말씀해주세요. -->

<!-- nest.js에서 인증을 구현할때 어떤 데코레이터를 썼나요. -->

새로운 기술을 도입하려 할때 본인만의 기준이 있는지 말씀해주세요.

<!-- 로컬에서 성능테스트나 벤치마킹같은걸 해보셨나요. -->

git에서 conflict가 났을 때 어떻게 처리하셨나요.

<!-- CI/CD가 뭐라고생각하시나요. CI와 CD의 차이점은 무엇인가요. -->

es6에서 구조 분해 할당에 대해서 설명해주세요.

let {a, b} = object 에서 a랑 b에 접근해서 수정할 경우 object의 실제값에 영향주나요.

만약에 a와 b가 또다른 객체라면 a랑 b를 수정할 경우 실제 값에 영향을 주나요.

db인덱싱에 대해서 설명해주세요.

userId와 taskId칼럼을 인덱싱할건데 어떻게 인덱싱을 할수있을까요.

인덱싱을 무작정 많이 적용해버리면 좋을까요.

<!-- nest.js의 각 layer에 대해 얘기해주세요. -->

<!-- 아마존에서 vpc의 개념을 설명해주세요. -->

select 요청이 많이 일어나는 서비스라면 어떤식으로 효율적으로 설계하실건가요.

<!-- stateless로 설계를 했을 때 어떤 장점이 있나요. -->

session - cookie 방식과 토큰 방식의 차이점에 대해 이야기해보세요

server의 역할에 대해 이야기해보세요

